<%= form_with(model: picture_test, local: true) do |form| %>
  <% if picture_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(picture_test.errors.count, "error") %> prohibited this picture_test from being saved:</h2>

      <ul>
      <% picture_test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
