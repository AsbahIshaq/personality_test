<%= form_for @objective_test do |f| %>
  <% if @objective_test.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@objective_test.errors.count, "error") %> prohibited
      this objective_test from being saved:</h2>
    <ul>
    <% @objective_test.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :question %><br>
    <%= f.text_field :question %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>